import { font_size, line_height } from './fonts';
import { colors } from './colors';
import { mixins } from './mixins';
import { spacing } from './spacing';

export const theme = {
  font_size,
  line_height,
  colors,
  mixins,
  spacing,
};
